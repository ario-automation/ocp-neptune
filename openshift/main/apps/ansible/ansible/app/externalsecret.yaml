---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: demo-gateway-postgres
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: demo-gateway-postgres-configuration
    template:
      engineVersion: v2
      data:
        database: "{{ .DATABASE }}"
        host: postgres-rw.database.svc.cluster.local
        password: "{{ .POSTGRES_PASS }}"
        port: "5432"
        type: unmanaged
        username: "{{ .POSTGRES_USER }}"
  dataFrom:
    - extract:
        key: automation-gateway
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: demo-controller-postgres
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: demo-controller-postgres-configuration
    template:
      engineVersion: v2
      data:
        database: "{{ .DATABASE }}"
        host: postgres-rw.database.svc.cluster.local
        password: "{{ .POSTGRES_PASS }}"
        port: "5432"
        type: unmanaged
        username: "{{ .POSTGRES_USER }}"
  dataFrom:
    - extract:
        key: automation-controller
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: demo-eda-postgres
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: demo-eda-postgres-configuration
    template:
      engineVersion: v2
      data:
        database: "{{ .DATABASE }}"
        host: postgres-rw.database.svc.cluster.local
        password: "{{ .POSTGRES_PASS }}"
        port: "5432"
        type: unmanaged
        username: "{{ .POSTGRES_USER }}"
  dataFrom:
    - extract:
        key: eda-controller
---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: demo-hub-postgres
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: demo-hub-postgres-configuration
    template:
      engineVersion: v2
      data:
        database: "{{ .DATABASE }}"
        host: postgres-rw.database.svc.cluster.local
        password: "{{ .POSTGRES_PASS }}"
        port: "5432"
        type: unmanaged
        username: "{{ .POSTGRES_USER }}"
  dataFrom:
    - extract:
        key: automation-hub
